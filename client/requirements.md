## Packages
(none needed beyond base stack)

## Notes
- Assume `@shared/schema` exports `loginSchema`, `User`, etc.
- Assume `@shared/routes` exports `api`, `errorSchemas`, `buildUrl`.
- Using standard Shadcn UI components for layout and styling.
